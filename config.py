config = {
    "logging_level":"INFO",
    "no_events":500,
    "write_time":0.25
}

logging_level = config["logging_level"]
no_events = int(config["no_events"])
write_time = float(config["write_time"])